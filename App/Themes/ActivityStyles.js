import { StyleSheet } from "react-native";

export const coverCompleteActivity = StyleSheet.create({
  globalPage: {
    width: "100%",
    height: "100%"
  }
});

export const centerAlignment = StyleSheet.create({
  centerAlignment: {
    alignItems: "center",
    justifyContent: "center",
    flex: 1,
    position: "relative"
  },
  contentAlignInCenter: {
    alignItems: "center"
  }
});
